import Database from "better-sqlite3";

const db = new Database("app.db");

db.prepare("CREATE TABLE IF NOT EXISTS users (id TEXT PRIMARY KEY, email TEXT)").run();

db.prepare("INSERT INTO users (id, email) VALUES (?, ?)").run("u1", "test@example.com");

const rows = db.prepare("SELECT * FROM users").all();
console.log(rows);
